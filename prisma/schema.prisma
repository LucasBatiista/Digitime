generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model provas {
  id    Int       @id
  nome  String?
  dia   DateTime? @db.Date
  hora  DateTime? @db.Time(6)
  local String?
}
